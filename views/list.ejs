<%- include("header") -%>

<div class="box" id="heading">
  <h1><%= routeTitle %></h1>
</div>

<div class="box">
  <% routeLists.forEach((item)=>{  %>
  <form action="/delete" method="post">
    <div class="item">
      <input type="checkbox" name="checkbox" value="<%= item._id %>" onchange="this.form.submit()" />
      <p><%= item.name %></p>
    </div>
    <input type="hidden" name="routeName" value="<%= routeTitle %>">
  </form>
 <%  } )%>

  <form class="item" action="/" method="post">
    <input
      type="text"
      name="newItem"
      placeholder="New Item"
      autocomplete="off"
    />
    <button type="submit" name="buttonRoute" value="<%= routeTitle %>">+</button>
  </form>
</div>

<%- include("footer") -%>
